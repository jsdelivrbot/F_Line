import Vue from 'vue';
Vue.prototype.$socketsign =function(message){
    //签名验证
    // var timestamp=(new Date()).valueOf();
    // var signdata=_rsa(this.$store.state.APPID+'&'+message);
    // return signdata
    return
}		

